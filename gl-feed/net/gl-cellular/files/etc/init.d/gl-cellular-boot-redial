#!/bin/sh /etc/rc.common

START=99
STOP=01

start() {
	# Do not hold up rcS while the modem registers.  The helper performs at
	# most one recovery redial and exits.
	/usr/sbin/gl-cellular-boot-redial >/dev/null 2>&1 &
}
